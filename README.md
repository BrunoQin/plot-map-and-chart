# Plot Map and Chart

```
brew install netcdf --with-cxx-compat % mac安装netcdf
pip install Cython
pip install netcdf4
pip install pillow
pip install geos
conda install matplotlib
pip install pyproj
conda install -c conda-forge basemap-data-hires=1.0.8.dev0
```